name: flutter_post_blog
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.5.0
  flutter_bloc: ^9.1.1
  equatable: ^2.0.8
  connectivity_plus: ^7.0.0
  http: ^1.6.0
  cloud_firestore: ^6.1.3
  firebase_auth: ^6.2.0
  intl: ^0.18.0
  get_it: ^7.7.0
  cached_network_image: ^3.2.3
  image_picker: ^1.2.1
  permission_handler: ^12.0.1
  app_settings: ^7.0.0
  firebase_storage: ^13.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.8
  bloc_test: ^10.0.0

flutter:
  uses-material-design: true
